<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose owns the in-app theming (see ui/theme/Theme.kt).
        This platform theme only styles the window before the first frame is drawn.
    -->
    <style name="Theme.CountryQuartet" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/brand_background</item>
        <item name="android:statusBarColor">@color/brand_background</item>
        <item name="android:navigationBarColor">@color/brand_background</item>
    </style>
</resources>
